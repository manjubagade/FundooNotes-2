using FundooApi.Controllers;
using System;
using Xunit;

namespace Testing
{
    public class UnitTest1
    {
        [Fact]
        
        public void Create()
        {
            // arrange
            //NotesController notesController = notesController.CreateNotes(Notes notesModel);

            // act/assert
            //Assert.Equal(2, abacus.Add(1, 1));
        }
    }
}
